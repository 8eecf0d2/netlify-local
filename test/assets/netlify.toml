[build]
  base = "default-base"
  publish = "default-publish"
  functions = "default-functions"
  command = "default-command"

[context.default]
  base = "default-base"
  publish = "default-publish"
  functions = "default-functions"
  command = "default-command"
